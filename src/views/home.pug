doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Zoom
        link(rel="stylesheet", href="https://unpkg.com/mvp.css")
    body
        header
            h1 Zoom
        main
            div#room
                form
                    input(type="text", placeholder="room name", requried)
                    button Enter Room
            div#chat
                h3
                ul 
                form
                    input(type="text", placeholder="message", requried)
                    button Send

        //- TODO 정적파일 js 를 ts 로 사용가능한지 확인 후 변경 예정
        script(src="/socket.io/socket.io.js") 
        script(src="/public/js/app.js") 